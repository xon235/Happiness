<?xml version="1.0" encoding="utf-8"?>
<layout>
    <data>
        <variable
            name="photoDetailFragment"
            type="com.family.happiness.ui.photodetail.PhotoDetailFragment" />
        <variable
            name="viewModel"
            type="com.family.happiness.ui.photodetail.PhotoDetailViewModel" />
    </data>
    <FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:tools="http://schemas.android.com/tools"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        tools:context="ui.PhotoDetailFragment"
        android:background="@color/black">

        <ProgressBar
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            app:visibleIf="@{!viewModel.inputEnabled}"/>

        <com.jsibbold.zoomage.ZoomageView
            android:id="@+id/myZoomageView"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            app:imageUrl="@{viewModel.photoDetail.photo.url}"
            app:zoomage_restrictBounds="false"
            app:zoomage_animateOnReset="true"
            app:zoomage_autoResetMode="UNDER"
            app:zoomage_autoCenter="true"
            app:zoomage_zoomable="true"
            app:zoomage_translatable="true"
            app:zoomage_minScale="0.6"
            app:zoomage_maxScale="8"
            app:enabledIf="@{viewModel.inputEnabled}"/>

        <ImageButton
            android:id="@+id/infoButton"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="end|bottom"
            android:src="@drawable/ic_info"
            android:background="@null"
            android:layout_margin="16dp"
            android:onClick="@{() -> photoDetailFragment.onClickInfo()}"/>

    </FrameLayout>
</layout>